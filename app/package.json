{
  "name": "universal-entity-explorer",
  "version": "0.1.0",
  "description": "Generic, schema-first, offline-capable platform for exploring and annotating structured data.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:html": "REPORT_PATH=$(node -e \"const fs=require('fs');const path=require('path');const dir='test_result';fs.mkdirSync(dir,{recursive:true});const ts=new Date().toISOString().replace(/[:.]/g,'-');process.stdout.write(path.join(dir,`test-${ts}.html`));\") && JEST_HTML_REPORTER_OUTPUT_PATH=$REPORT_PATH NODE_OPTIONS=--experimental-vm-modules jest --reporters=default --reporters=jest-html-reporter",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "dev": "node scripts/dev-server.js",
    "lint": "eslint src/ test/ --fix",
    "format": "prettier --write \"src/**/*.js\" \"test/**/*.js\""
  },
  "keywords": [
    "graph",
    "entity-explorer",
    "offline-first",
    "schema-driven",
    "web-components"
  ],
  "author": "bhagwati-prasad",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@testing-library/dom": "^9.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-html-reporter": "^4.3.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
